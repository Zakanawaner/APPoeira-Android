<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="end">

    <SeekBar
        android:id="@+id/distanceBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#efefef"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="@+id/distanceButton"
        app:layout_constraintEnd_toStartOf="@+id/distanceButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/distanceButton" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/distanceButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/swipeRefreshList"
        android:layout_alignParentEnd="true"
        android:backgroundTint="@color/colorWhite"
        android:src="@drawable/ic_road"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/tvDistanceInKm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:elevation="7dp"
        android:gravity="center"
        android:textAlignment="center"
        android:textSize="18sp"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="@+id/distanceButton"
        app:layout_constraintEnd_toEndOf="@+id/distanceButton"
        app:layout_constraintStart_toStartOf="@+id/distanceButton"
        app:layout_constraintTop_toTopOf="@+id/distanceButton" />
</androidx.constraintlayout.widget.ConstraintLayout>